# Python_Encoder
A python tool to encode your  payloads, very useful to bypass the WAF in bug bounties, it let's you choose between different types of encoding, such as URL, Double URL, HTML, Unicode, Base64, UTF-7. It can even generate a file.txt containing a list of different variants of the encoded payload with capital letters permutations. 
